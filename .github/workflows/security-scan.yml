name: Security CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  secret-scan:
    name: Secret Detection
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: TruffleHog Secret Scan (baseline)
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./ 
          extra_args: --only-verified --json
        continue-on-error: true

  sast-scan:
    name: Static Code Analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run Semgrep (security audit – baseline)
        uses: returntocorp/semgrep-action@v1
        with:
          config: p/security-audit
        continue-on-error: true

  container-scan:
    name: Container Security Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker image
        run: docker build -t secure-app:${{ github.sha }} .

      - name: Run Trivy and generate SARIF
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: secure-app:${{ github.sha }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH,MEDIUM'
          exit-code: '0'  # baseline: do not fail build yet

      - name: Upload Trivy results to Security tab
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-results.sarif'
  dast-scan:
    name: DAST Scan (OWASP ZAP)
    runs-on: ubuntu-latest
    needs: [sast-scan, container-scan]
    permissions:
      contents: read
      issues: write      # allow ZAP action to create/update an Issue with findings
    steps:
      - name: Run OWASP ZAP full scan
        uses: zaproxy/action-full-scan@v0.13.0
        continue-on-error: true      # do NOT fail the workflow if artifact upload breaks
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target: ${{ secrets.DAST_TARGET_URL }}   # your full URL, already set as secret
          cmd_options: "-a"
          artifact_name: "zap"       # or zap_report, zap_scan – name is fine, backend just doesn't like artifacts
          allow_issue_writing: true  # default; ZAP writes findings into a GitHub Issue
          fail_action: false         # don't fail pipeline on alerts (good for POC/baseline)
